# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.setup_intent import SetupIntent
from ..types.setup_intent_list import SetupIntentList
from .raw_client import AsyncRawSetupIntentsClient, RawSetupIntentsClient
from .types.cancel_setup_intent_request_cancellation_reason import CancelSetupIntentRequestCancellationReason
from .types.create_setup_intent_request_usage import CreateSetupIntentRequestUsage
from .types.list_setup_intents_request_status import ListSetupIntentsRequestStatus

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class SetupIntentsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawSetupIntentsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawSetupIntentsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawSetupIntentsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        customer_id: typing.Optional[str] = None,
        status: typing.Optional[ListSetupIntentsRequestStatus] = None,
        limit: typing.Optional[int] = None,
        starting_after: typing.Optional[str] = None,
        ending_before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SetupIntentList:
        """
        Returns a list of setup intents. The setup intents are returned sorted by creation date, with the most recent first.

        Parameters
        ----------
        customer_id : typing.Optional[str]
            Filter by customer ID

        status : typing.Optional[ListSetupIntentsRequestStatus]
            Filter by status

        limit : typing.Optional[int]
            Maximum number of results to return

        starting_after : typing.Optional[str]
            Cursor for pagination (setup intent ID)

        ending_before : typing.Optional[str]
            Cursor for pagination (setup intent ID)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SetupIntentList
            List of setup intents

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.setup_intents.list()
        """
        _response = self._raw_client.list(
            customer_id=customer_id,
            status=status,
            limit=limit,
            starting_after=starting_after,
            ending_before=ending_before,
            request_options=request_options,
        )
        return _response.data

    def create(
        self,
        *,
        customer_id: typing.Optional[str] = OMIT,
        payment_method_types: typing.Optional[typing.Sequence[str]] = OMIT,
        usage: typing.Optional[CreateSetupIntentRequestUsage] = OMIT,
        description: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SetupIntent:
        """
        Creates a new setup intent for saving a payment method without charging.

        **Use Cases:**
        - Save a card for future subscription payments
        - Pre-authorize a payment method with 3DS/SCA verification
        - Collect payment details for later use (off-session payments)

        The setup intent is created in `requires_payment_method` status. Use the `client_secret` to complete the setup flow on the client side.

        Parameters
        ----------
        customer_id : typing.Optional[str]
            ID of the customer to attach the payment method to

        payment_method_types : typing.Optional[typing.Sequence[str]]
            Allowed payment method types

        usage : typing.Optional[CreateSetupIntentRequestUsage]
            How the payment method will be used

        description : typing.Optional[str]
            Merchant description for reference

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SetupIntent
            Setup intent created successfully

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.setup_intents.create()
        """
        _response = self._raw_client.create(
            customer_id=customer_id,
            payment_method_types=payment_method_types,
            usage=usage,
            description=description,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> SetupIntent:
        """
        Retrieves a setup intent by ID. You can use either the internal UUID or the public ID (seti_xxx format).

        Parameters
        ----------
        id : str
            Setup Intent ID (UUID or public ID seti_xxx)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SetupIntent
            Setup intent found

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.setup_intents.retrieve(
            id="seti_1a2b3c4d5e6f",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    def update(
        self,
        id: str,
        *,
        customer_id: typing.Optional[str] = OMIT,
        payment_method_types: typing.Optional[typing.Sequence[str]] = OMIT,
        description: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SetupIntent:
        """
        Updates a setup intent that has not yet been confirmed or canceled.

        You can update:
        - `customer_id` - The customer to attach the payment method to
        - `payment_method_types` - Allowed payment method types
        - `description` - Merchant description
        - `metadata` - Custom metadata

        Parameters
        ----------
        id : str
            Setup Intent ID (UUID or public ID seti_xxx)

        customer_id : typing.Optional[str]
            ID of the customer to attach the payment method to

        payment_method_types : typing.Optional[typing.Sequence[str]]
            Allowed payment method types

        description : typing.Optional[str]
            Merchant description for reference

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SetupIntent
            Setup intent updated

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.setup_intents.update(
            id="seti_1a2b3c4d5e6f",
        )
        """
        _response = self._raw_client.update(
            id,
            customer_id=customer_id,
            payment_method_types=payment_method_types,
            description=description,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    def confirm(
        self,
        id: str,
        *,
        payment_method_id: typing.Optional[str] = OMIT,
        return_url: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SetupIntent:
        """
        Confirms a setup intent and saves the payment method.

        **3DS/SCA Flow:**
        If the payment method requires authentication, the setup intent will transition to `requires_action` status with a `next_action` containing the redirect URL. After the customer completes authentication, the setup intent will automatically transition to `succeeded`.

        **Direct Confirmation:**
        If no authentication is required, the setup intent will immediately transition to `succeeded` and the payment method will be saved to the customer.

        Parameters
        ----------
        id : str
            Setup Intent ID (UUID or public ID seti_xxx)

        payment_method_id : typing.Optional[str]
            ID of the payment method to confirm with

        return_url : typing.Optional[str]
            URL to redirect to after 3DS authentication

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SetupIntent
            Setup intent confirmed (or requires action)

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.setup_intents.confirm(
            id="seti_1a2b3c4d5e6f",
        )
        """
        _response = self._raw_client.confirm(
            id, payment_method_id=payment_method_id, return_url=return_url, request_options=request_options
        )
        return _response.data

    def cancel(
        self,
        id: str,
        *,
        cancellation_reason: typing.Optional[CancelSetupIntentRequestCancellationReason] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SetupIntent:
        """
        Cancels a setup intent that has not yet succeeded.

        Once canceled, the setup intent cannot be confirmed. A new setup intent must be created to save a payment method.

        Parameters
        ----------
        id : str
            Setup Intent ID (UUID or public ID seti_xxx)

        cancellation_reason : typing.Optional[CancelSetupIntentRequestCancellationReason]
            Reason for cancellation

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SetupIntent
            Setup intent canceled

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.setup_intents.cancel(
            id="seti_1a2b3c4d5e6f",
        )
        """
        _response = self._raw_client.cancel(
            id, cancellation_reason=cancellation_reason, request_options=request_options
        )
        return _response.data


class AsyncSetupIntentsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawSetupIntentsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawSetupIntentsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawSetupIntentsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        customer_id: typing.Optional[str] = None,
        status: typing.Optional[ListSetupIntentsRequestStatus] = None,
        limit: typing.Optional[int] = None,
        starting_after: typing.Optional[str] = None,
        ending_before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SetupIntentList:
        """
        Returns a list of setup intents. The setup intents are returned sorted by creation date, with the most recent first.

        Parameters
        ----------
        customer_id : typing.Optional[str]
            Filter by customer ID

        status : typing.Optional[ListSetupIntentsRequestStatus]
            Filter by status

        limit : typing.Optional[int]
            Maximum number of results to return

        starting_after : typing.Optional[str]
            Cursor for pagination (setup intent ID)

        ending_before : typing.Optional[str]
            Cursor for pagination (setup intent ID)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SetupIntentList
            List of setup intents

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.setup_intents.list()


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            customer_id=customer_id,
            status=status,
            limit=limit,
            starting_after=starting_after,
            ending_before=ending_before,
            request_options=request_options,
        )
        return _response.data

    async def create(
        self,
        *,
        customer_id: typing.Optional[str] = OMIT,
        payment_method_types: typing.Optional[typing.Sequence[str]] = OMIT,
        usage: typing.Optional[CreateSetupIntentRequestUsage] = OMIT,
        description: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SetupIntent:
        """
        Creates a new setup intent for saving a payment method without charging.

        **Use Cases:**
        - Save a card for future subscription payments
        - Pre-authorize a payment method with 3DS/SCA verification
        - Collect payment details for later use (off-session payments)

        The setup intent is created in `requires_payment_method` status. Use the `client_secret` to complete the setup flow on the client side.

        Parameters
        ----------
        customer_id : typing.Optional[str]
            ID of the customer to attach the payment method to

        payment_method_types : typing.Optional[typing.Sequence[str]]
            Allowed payment method types

        usage : typing.Optional[CreateSetupIntentRequestUsage]
            How the payment method will be used

        description : typing.Optional[str]
            Merchant description for reference

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SetupIntent
            Setup intent created successfully

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.setup_intents.create()


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            customer_id=customer_id,
            payment_method_types=payment_method_types,
            usage=usage,
            description=description,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> SetupIntent:
        """
        Retrieves a setup intent by ID. You can use either the internal UUID or the public ID (seti_xxx format).

        Parameters
        ----------
        id : str
            Setup Intent ID (UUID or public ID seti_xxx)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SetupIntent
            Setup intent found

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.setup_intents.retrieve(
                id="seti_1a2b3c4d5e6f",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    async def update(
        self,
        id: str,
        *,
        customer_id: typing.Optional[str] = OMIT,
        payment_method_types: typing.Optional[typing.Sequence[str]] = OMIT,
        description: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SetupIntent:
        """
        Updates a setup intent that has not yet been confirmed or canceled.

        You can update:
        - `customer_id` - The customer to attach the payment method to
        - `payment_method_types` - Allowed payment method types
        - `description` - Merchant description
        - `metadata` - Custom metadata

        Parameters
        ----------
        id : str
            Setup Intent ID (UUID or public ID seti_xxx)

        customer_id : typing.Optional[str]
            ID of the customer to attach the payment method to

        payment_method_types : typing.Optional[typing.Sequence[str]]
            Allowed payment method types

        description : typing.Optional[str]
            Merchant description for reference

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SetupIntent
            Setup intent updated

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.setup_intents.update(
                id="seti_1a2b3c4d5e6f",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            id,
            customer_id=customer_id,
            payment_method_types=payment_method_types,
            description=description,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    async def confirm(
        self,
        id: str,
        *,
        payment_method_id: typing.Optional[str] = OMIT,
        return_url: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SetupIntent:
        """
        Confirms a setup intent and saves the payment method.

        **3DS/SCA Flow:**
        If the payment method requires authentication, the setup intent will transition to `requires_action` status with a `next_action` containing the redirect URL. After the customer completes authentication, the setup intent will automatically transition to `succeeded`.

        **Direct Confirmation:**
        If no authentication is required, the setup intent will immediately transition to `succeeded` and the payment method will be saved to the customer.

        Parameters
        ----------
        id : str
            Setup Intent ID (UUID or public ID seti_xxx)

        payment_method_id : typing.Optional[str]
            ID of the payment method to confirm with

        return_url : typing.Optional[str]
            URL to redirect to after 3DS authentication

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SetupIntent
            Setup intent confirmed (or requires action)

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.setup_intents.confirm(
                id="seti_1a2b3c4d5e6f",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.confirm(
            id, payment_method_id=payment_method_id, return_url=return_url, request_options=request_options
        )
        return _response.data

    async def cancel(
        self,
        id: str,
        *,
        cancellation_reason: typing.Optional[CancelSetupIntentRequestCancellationReason] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SetupIntent:
        """
        Cancels a setup intent that has not yet succeeded.

        Once canceled, the setup intent cannot be confirmed. A new setup intent must be created to save a payment method.

        Parameters
        ----------
        id : str
            Setup Intent ID (UUID or public ID seti_xxx)

        cancellation_reason : typing.Optional[CancelSetupIntentRequestCancellationReason]
            Reason for cancellation

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SetupIntent
            Setup intent canceled

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.setup_intents.cancel(
                id="seti_1a2b3c4d5e6f",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.cancel(
            id, cancellation_reason=cancellation_reason, request_options=request_options
        )
        return _response.data
