# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.create_meter_response import CreateMeterResponse
from ..types.meter_get_response import MeterGetResponse
from ..types.meter_list_response import MeterListResponse
from ..types.meter_price_list_response import MeterPriceListResponse
from ..types.meter_price_response import MeterPriceResponse
from ..types.update_meter_response import UpdateMeterResponse
from .raw_client import AsyncRawMetersClient, RawMetersClient
from .types.create_meter_price_request_pricing_model import CreateMeterPriceRequestPricingModel
from .types.create_meter_price_request_tiers_item import CreateMeterPriceRequestTiersItem
from .types.create_meter_request_aggregation import CreateMeterRequestAggregation
from .types.update_meter_price_request_tiers_item import UpdateMeterPriceRequestTiersItem
from .types.update_meter_request_status import UpdateMeterRequestStatus

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class MetersClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawMetersClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawMetersClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawMetersClient
        """
        return self._raw_client

    def list(
        self,
        *,
        include_archived: typing.Optional[str] = None,
        limit: typing.Optional[str] = None,
        offset: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MeterListResponse:
        """
        List all usage meters with optional filtering and pagination.

        Parameters
        ----------
        include_archived : typing.Optional[str]
            Include archived meters

        limit : typing.Optional[str]
            Max results (default 50, max 100)

        offset : typing.Optional[str]
            Pagination offset

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MeterListResponse
            List of meters

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.meters.list(
            include_archived="false",
            limit="50",
            offset="0",
        )
        """
        _response = self._raw_client.list(
            include_archived=include_archived, limit=limit, offset=offset, request_options=request_options
        )
        return _response.data

    def create(
        self,
        *,
        name: str,
        event_name: str,
        aggregation: typing.Optional[CreateMeterRequestAggregation] = OMIT,
        slug: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        value_key: typing.Optional[str] = OMIT,
        filter_conditions: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        unique_count_key: typing.Optional[str] = OMIT,
        unit_name: typing.Optional[str] = OMIT,
        carry_forward: typing.Optional[bool] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        alert_thresholds: typing.Optional[typing.Sequence[float]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateMeterResponse:
        """
        Create a new usage meter. Meters define how usage events are aggregated for billing.

        Parameters
        ----------
        name : str
            Human-readable meter name

        event_name : str
            Event name to match against

        aggregation : typing.Optional[CreateMeterRequestAggregation]
            Aggregation method

        slug : typing.Optional[str]
            URL-safe identifier

        description : typing.Optional[str]
            Meter description

        value_key : typing.Optional[str]
            Property key for sum/max/last aggregations

        filter_conditions : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]
            Filter conditions for matching events

        unique_count_key : typing.Optional[str]
            Property key for count_unique aggregation

        unit_name : typing.Optional[str]
            Display unit name

        carry_forward : typing.Optional[bool]
            Carry forward last value across periods

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Arbitrary key-value metadata

        alert_thresholds : typing.Optional[typing.Sequence[float]]
            Threshold percentages that trigger usage.threshold.reached webhooks.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateMeterResponse
            Meter created

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.meters.create(
            name="API Calls",
            event_name="api_call",
        )
        """
        _response = self._raw_client.create(
            name=name,
            event_name=event_name,
            aggregation=aggregation,
            slug=slug,
            description=description,
            value_key=value_key,
            filter_conditions=filter_conditions,
            unique_count_key=unique_count_key,
            unit_name=unit_name,
            carry_forward=carry_forward,
            metadata=metadata,
            alert_thresholds=alert_thresholds,
            request_options=request_options,
        )
        return _response.data

    def get(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> MeterGetResponse:
        """
        Retrieve a single meter by ID.

        Parameters
        ----------
        id : str
            Meter ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MeterGetResponse
            Meter details

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.meters.get(
            id="mtr_abc123",
        )
        """
        _response = self._raw_client.get(id, request_options=request_options)
        return _response.data

    def update(
        self,
        id: str,
        *,
        name: typing.Optional[str] = OMIT,
        slug: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        status: typing.Optional[UpdateMeterRequestStatus] = OMIT,
        value_key: typing.Optional[str] = OMIT,
        filter_conditions: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        unique_count_key: typing.Optional[str] = OMIT,
        unit_name: typing.Optional[str] = OMIT,
        carry_forward: typing.Optional[bool] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        alert_thresholds: typing.Optional[typing.Sequence[float]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateMeterResponse:
        """
        Update a meter's configuration. Note: `event_name` and `aggregation` are immutable after creation.

        Parameters
        ----------
        id : str
            Meter ID

        name : typing.Optional[str]
            Updated meter name

        slug : typing.Optional[str]
            Updated slug

        description : typing.Optional[str]
            Updated description

        status : typing.Optional[UpdateMeterRequestStatus]
            Meter status

        value_key : typing.Optional[str]
            Updated value key

        filter_conditions : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]
            Updated filter conditions

        unique_count_key : typing.Optional[str]
            Updated unique count key

        unit_name : typing.Optional[str]
            Updated unit name

        carry_forward : typing.Optional[bool]
            Updated carry forward setting

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Updated metadata

        alert_thresholds : typing.Optional[typing.Sequence[float]]
            Updated threshold percentages for usage.threshold.reached webhooks.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateMeterResponse
            Meter updated

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.meters.update(
            id="mtr_abc123",
        )
        """
        _response = self._raw_client.update(
            id,
            name=name,
            slug=slug,
            description=description,
            status=status,
            value_key=value_key,
            filter_conditions=filter_conditions,
            unique_count_key=unique_count_key,
            unit_name=unit_name,
            carry_forward=carry_forward,
            metadata=metadata,
            alert_thresholds=alert_thresholds,
            request_options=request_options,
        )
        return _response.data

    def list_prices(
        self, meter_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> MeterPriceListResponse:
        """
        List all prices attached to a meter, including their tier configurations.

        Parameters
        ----------
        meter_id : str
            Meter ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MeterPriceListResponse
            List of prices

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.meters.list_prices(
            meter_id="meterId",
        )
        """
        _response = self._raw_client.list_prices(meter_id, request_options=request_options)
        return _response.data

    def create_price(
        self,
        meter_id: str,
        *,
        pricing_model: CreateMeterPriceRequestPricingModel,
        currency: str,
        unit_amount_minor: typing.Optional[int] = OMIT,
        flat_fee_minor: typing.Optional[int] = OMIT,
        package_size: typing.Optional[int] = OMIT,
        cost_per_unit_minor: typing.Optional[int] = OMIT,
        tiers: typing.Optional[typing.Sequence[CreateMeterPriceRequestTiersItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MeterPriceResponse:
        """
        Create a usage price for a meter with a specific pricing model (per_unit, graduated, volume, or package).

        Parameters
        ----------
        meter_id : str
            Meter ID

        pricing_model : CreateMeterPriceRequestPricingModel
            Pricing model

        currency : str
            ISO 4217 currency code

        unit_amount_minor : typing.Optional[int]
            Price per unit in minor units (for per_unit)

        flat_fee_minor : typing.Optional[int]
            Base charge per period in minor units

        package_size : typing.Optional[int]
            Units per package (for package model)

        cost_per_unit_minor : typing.Optional[int]
            Merchant's cost per unit for margin intelligence

        tiers : typing.Optional[typing.Sequence[CreateMeterPriceRequestTiersItem]]
            Tier configuration (for graduated and volume)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MeterPriceResponse
            Price created

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.meters.create_price(
            meter_id="meterId",
            pricing_model="per_unit",
            currency="GBP",
        )
        """
        _response = self._raw_client.create_price(
            meter_id,
            pricing_model=pricing_model,
            currency=currency,
            unit_amount_minor=unit_amount_minor,
            flat_fee_minor=flat_fee_minor,
            package_size=package_size,
            cost_per_unit_minor=cost_per_unit_minor,
            tiers=tiers,
            request_options=request_options,
        )
        return _response.data

    def update_price(
        self,
        meter_id: str,
        price_id: str,
        *,
        unit_amount_minor: typing.Optional[int] = OMIT,
        flat_fee_minor: typing.Optional[int] = OMIT,
        package_size: typing.Optional[int] = OMIT,
        cost_per_unit_minor: typing.Optional[int] = OMIT,
        tiers: typing.Optional[typing.Sequence[UpdateMeterPriceRequestTiersItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MeterPriceResponse:
        """
        Update an existing usage price configuration. The pricing_model is immutable.

        Parameters
        ----------
        meter_id : str
            Meter ID

        price_id : str
            Price ID

        unit_amount_minor : typing.Optional[int]

        flat_fee_minor : typing.Optional[int]

        package_size : typing.Optional[int]

        cost_per_unit_minor : typing.Optional[int]

        tiers : typing.Optional[typing.Sequence[UpdateMeterPriceRequestTiersItem]]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MeterPriceResponse
            Price updated

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.meters.update_price(
            meter_id="meterId",
            price_id="priceId",
        )
        """
        _response = self._raw_client.update_price(
            meter_id,
            price_id,
            unit_amount_minor=unit_amount_minor,
            flat_fee_minor=flat_fee_minor,
            package_size=package_size,
            cost_per_unit_minor=cost_per_unit_minor,
            tiers=tiers,
            request_options=request_options,
        )
        return _response.data

    def deactivate_price(
        self, meter_id: str, price_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> MeterPriceResponse:
        """
        Deactivate a usage price. Deactivated prices cannot be used for new subscriptions but existing subscriptions continue until the end of their billing period.

        Parameters
        ----------
        meter_id : str
            Meter ID

        price_id : str
            Price ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MeterPriceResponse
            Price deactivated

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.meters.deactivate_price(
            meter_id="meterId",
            price_id="priceId",
        )
        """
        _response = self._raw_client.deactivate_price(meter_id, price_id, request_options=request_options)
        return _response.data


class AsyncMetersClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawMetersClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawMetersClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawMetersClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        include_archived: typing.Optional[str] = None,
        limit: typing.Optional[str] = None,
        offset: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MeterListResponse:
        """
        List all usage meters with optional filtering and pagination.

        Parameters
        ----------
        include_archived : typing.Optional[str]
            Include archived meters

        limit : typing.Optional[str]
            Max results (default 50, max 100)

        offset : typing.Optional[str]
            Pagination offset

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MeterListResponse
            List of meters

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.meters.list(
                include_archived="false",
                limit="50",
                offset="0",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            include_archived=include_archived, limit=limit, offset=offset, request_options=request_options
        )
        return _response.data

    async def create(
        self,
        *,
        name: str,
        event_name: str,
        aggregation: typing.Optional[CreateMeterRequestAggregation] = OMIT,
        slug: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        value_key: typing.Optional[str] = OMIT,
        filter_conditions: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        unique_count_key: typing.Optional[str] = OMIT,
        unit_name: typing.Optional[str] = OMIT,
        carry_forward: typing.Optional[bool] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        alert_thresholds: typing.Optional[typing.Sequence[float]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateMeterResponse:
        """
        Create a new usage meter. Meters define how usage events are aggregated for billing.

        Parameters
        ----------
        name : str
            Human-readable meter name

        event_name : str
            Event name to match against

        aggregation : typing.Optional[CreateMeterRequestAggregation]
            Aggregation method

        slug : typing.Optional[str]
            URL-safe identifier

        description : typing.Optional[str]
            Meter description

        value_key : typing.Optional[str]
            Property key for sum/max/last aggregations

        filter_conditions : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]
            Filter conditions for matching events

        unique_count_key : typing.Optional[str]
            Property key for count_unique aggregation

        unit_name : typing.Optional[str]
            Display unit name

        carry_forward : typing.Optional[bool]
            Carry forward last value across periods

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Arbitrary key-value metadata

        alert_thresholds : typing.Optional[typing.Sequence[float]]
            Threshold percentages that trigger usage.threshold.reached webhooks.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateMeterResponse
            Meter created

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.meters.create(
                name="API Calls",
                event_name="api_call",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            name=name,
            event_name=event_name,
            aggregation=aggregation,
            slug=slug,
            description=description,
            value_key=value_key,
            filter_conditions=filter_conditions,
            unique_count_key=unique_count_key,
            unit_name=unit_name,
            carry_forward=carry_forward,
            metadata=metadata,
            alert_thresholds=alert_thresholds,
            request_options=request_options,
        )
        return _response.data

    async def get(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> MeterGetResponse:
        """
        Retrieve a single meter by ID.

        Parameters
        ----------
        id : str
            Meter ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MeterGetResponse
            Meter details

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.meters.get(
                id="mtr_abc123",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(id, request_options=request_options)
        return _response.data

    async def update(
        self,
        id: str,
        *,
        name: typing.Optional[str] = OMIT,
        slug: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        status: typing.Optional[UpdateMeterRequestStatus] = OMIT,
        value_key: typing.Optional[str] = OMIT,
        filter_conditions: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        unique_count_key: typing.Optional[str] = OMIT,
        unit_name: typing.Optional[str] = OMIT,
        carry_forward: typing.Optional[bool] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        alert_thresholds: typing.Optional[typing.Sequence[float]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateMeterResponse:
        """
        Update a meter's configuration. Note: `event_name` and `aggregation` are immutable after creation.

        Parameters
        ----------
        id : str
            Meter ID

        name : typing.Optional[str]
            Updated meter name

        slug : typing.Optional[str]
            Updated slug

        description : typing.Optional[str]
            Updated description

        status : typing.Optional[UpdateMeterRequestStatus]
            Meter status

        value_key : typing.Optional[str]
            Updated value key

        filter_conditions : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]
            Updated filter conditions

        unique_count_key : typing.Optional[str]
            Updated unique count key

        unit_name : typing.Optional[str]
            Updated unit name

        carry_forward : typing.Optional[bool]
            Updated carry forward setting

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Updated metadata

        alert_thresholds : typing.Optional[typing.Sequence[float]]
            Updated threshold percentages for usage.threshold.reached webhooks.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateMeterResponse
            Meter updated

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.meters.update(
                id="mtr_abc123",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            id,
            name=name,
            slug=slug,
            description=description,
            status=status,
            value_key=value_key,
            filter_conditions=filter_conditions,
            unique_count_key=unique_count_key,
            unit_name=unit_name,
            carry_forward=carry_forward,
            metadata=metadata,
            alert_thresholds=alert_thresholds,
            request_options=request_options,
        )
        return _response.data

    async def list_prices(
        self, meter_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> MeterPriceListResponse:
        """
        List all prices attached to a meter, including their tier configurations.

        Parameters
        ----------
        meter_id : str
            Meter ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MeterPriceListResponse
            List of prices

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.meters.list_prices(
                meter_id="meterId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_prices(meter_id, request_options=request_options)
        return _response.data

    async def create_price(
        self,
        meter_id: str,
        *,
        pricing_model: CreateMeterPriceRequestPricingModel,
        currency: str,
        unit_amount_minor: typing.Optional[int] = OMIT,
        flat_fee_minor: typing.Optional[int] = OMIT,
        package_size: typing.Optional[int] = OMIT,
        cost_per_unit_minor: typing.Optional[int] = OMIT,
        tiers: typing.Optional[typing.Sequence[CreateMeterPriceRequestTiersItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MeterPriceResponse:
        """
        Create a usage price for a meter with a specific pricing model (per_unit, graduated, volume, or package).

        Parameters
        ----------
        meter_id : str
            Meter ID

        pricing_model : CreateMeterPriceRequestPricingModel
            Pricing model

        currency : str
            ISO 4217 currency code

        unit_amount_minor : typing.Optional[int]
            Price per unit in minor units (for per_unit)

        flat_fee_minor : typing.Optional[int]
            Base charge per period in minor units

        package_size : typing.Optional[int]
            Units per package (for package model)

        cost_per_unit_minor : typing.Optional[int]
            Merchant's cost per unit for margin intelligence

        tiers : typing.Optional[typing.Sequence[CreateMeterPriceRequestTiersItem]]
            Tier configuration (for graduated and volume)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MeterPriceResponse
            Price created

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.meters.create_price(
                meter_id="meterId",
                pricing_model="per_unit",
                currency="GBP",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_price(
            meter_id,
            pricing_model=pricing_model,
            currency=currency,
            unit_amount_minor=unit_amount_minor,
            flat_fee_minor=flat_fee_minor,
            package_size=package_size,
            cost_per_unit_minor=cost_per_unit_minor,
            tiers=tiers,
            request_options=request_options,
        )
        return _response.data

    async def update_price(
        self,
        meter_id: str,
        price_id: str,
        *,
        unit_amount_minor: typing.Optional[int] = OMIT,
        flat_fee_minor: typing.Optional[int] = OMIT,
        package_size: typing.Optional[int] = OMIT,
        cost_per_unit_minor: typing.Optional[int] = OMIT,
        tiers: typing.Optional[typing.Sequence[UpdateMeterPriceRequestTiersItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MeterPriceResponse:
        """
        Update an existing usage price configuration. The pricing_model is immutable.

        Parameters
        ----------
        meter_id : str
            Meter ID

        price_id : str
            Price ID

        unit_amount_minor : typing.Optional[int]

        flat_fee_minor : typing.Optional[int]

        package_size : typing.Optional[int]

        cost_per_unit_minor : typing.Optional[int]

        tiers : typing.Optional[typing.Sequence[UpdateMeterPriceRequestTiersItem]]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MeterPriceResponse
            Price updated

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.meters.update_price(
                meter_id="meterId",
                price_id="priceId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_price(
            meter_id,
            price_id,
            unit_amount_minor=unit_amount_minor,
            flat_fee_minor=flat_fee_minor,
            package_size=package_size,
            cost_per_unit_minor=cost_per_unit_minor,
            tiers=tiers,
            request_options=request_options,
        )
        return _response.data

    async def deactivate_price(
        self, meter_id: str, price_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> MeterPriceResponse:
        """
        Deactivate a usage price. Deactivated prices cannot be used for new subscriptions but existing subscriptions continue until the end of their billing period.

        Parameters
        ----------
        meter_id : str
            Meter ID

        price_id : str
            Price ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MeterPriceResponse
            Price deactivated

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.meters.deactivate_price(
                meter_id="meterId",
                price_id="priceId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.deactivate_price(meter_id, price_id, request_options=request_options)
        return _response.data
