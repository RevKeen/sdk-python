# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.price import Price
from ..types.price_list_response import PriceListResponse
from .raw_client import AsyncRawPricesClient, RawPricesClient
from .types.create_price_request_interval import CreatePriceRequestInterval
from .types.create_price_request_pricing_model import CreatePriceRequestPricingModel
from .types.create_price_request_type import CreatePriceRequestType
from .types.prices_list_request_active import PricesListRequestActive
from .types.prices_list_request_pricing_model import PricesListRequestPricingModel
from .types.prices_list_request_type import PricesListRequestType

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class PricesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawPricesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawPricesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawPricesClient
        """
        return self._raw_client

    def list(
        self,
        *,
        product_id: typing.Optional[str] = None,
        type: typing.Optional[PricesListRequestType] = None,
        pricing_model: typing.Optional[PricesListRequestPricingModel] = None,
        active: typing.Optional[PricesListRequestActive] = None,
        currency: typing.Optional[str] = None,
        lookup_key: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        starting_after: typing.Optional[str] = None,
        ending_before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PriceListResponse:
        """
        List all prices for the authenticated merchant. Supports filtering and cursor-based pagination.

        Parameters
        ----------
        product_id : typing.Optional[str]
            Filter by product ID

        type : typing.Optional[PricesListRequestType]
            Filter by price type

        pricing_model : typing.Optional[PricesListRequestPricingModel]
            Filter by pricing model

        active : typing.Optional[PricesListRequestActive]
            Filter by active status

        currency : typing.Optional[str]
            Filter by currency

        lookup_key : typing.Optional[str]
            Filter by lookup key

        limit : typing.Optional[int]
            Number of results (1-100)

        starting_after : typing.Optional[str]
            Cursor for pagination (ID to start after)

        ending_before : typing.Optional[str]
            Cursor for pagination (ID to end before)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PriceListResponse
            List of prices

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.prices.list()
        """
        _response = self._raw_client.list(
            product_id=product_id,
            type=type,
            pricing_model=pricing_model,
            active=active,
            currency=currency,
            lookup_key=lookup_key,
            limit=limit,
            starting_after=starting_after,
            ending_before=ending_before,
            request_options=request_options,
        )
        return _response.data

    def create(
        self,
        *,
        product_id: str,
        currency: typing.Optional[str] = OMIT,
        unit_amount: typing.Optional[int] = OMIT,
        type: typing.Optional[CreatePriceRequestType] = OMIT,
        pricing_model: typing.Optional[CreatePriceRequestPricingModel] = OMIT,
        interval: typing.Optional[CreatePriceRequestInterval] = OMIT,
        interval_count: typing.Optional[int] = OMIT,
        trial_period_days: typing.Optional[int] = OMIT,
        minimum_amount: typing.Optional[int] = OMIT,
        maximum_amount: typing.Optional[int] = OMIT,
        suggested_amount: typing.Optional[int] = OMIT,
        preset_amounts: typing.Optional[typing.Sequence[int]] = OMIT,
        nickname: typing.Optional[str] = OMIT,
        lookup_key: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Price:
        """
        Creates a new price for a product. Supports fixed, pay-what-you-want, and free pricing models.

        Parameters
        ----------
        product_id : str
            ID of the product this price belongs to

        currency : typing.Optional[str]
            Three-letter ISO currency code

        unit_amount : typing.Optional[int]
            Price in minor units (cents). Required for fixed pricing.

        type : typing.Optional[CreatePriceRequestType]
            Price type

        pricing_model : typing.Optional[CreatePriceRequestPricingModel]
            Pricing model

        interval : typing.Optional[CreatePriceRequestInterval]
            Billing interval (required for recurring)

        interval_count : typing.Optional[int]
            Number of intervals between billings (1-12)

        trial_period_days : typing.Optional[int]
            Trial period in days (0-730)

        minimum_amount : typing.Optional[int]
            PWYW: minimum amount in cents

        maximum_amount : typing.Optional[int]
            PWYW: maximum amount in cents

        suggested_amount : typing.Optional[int]
            PWYW: suggested amount in cents

        preset_amounts : typing.Optional[typing.Sequence[int]]
            PWYW: quick-select amounts (max 10)

        nickname : typing.Optional[str]
            Display name (e.g., 'Monthly', 'Annual')

        lookup_key : typing.Optional[str]
            Stable key for API lookups

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom key-value metadata

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Price
            Price created successfully

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.prices.create(
            product_id="product_id",
        )
        """
        _response = self._raw_client.create(
            product_id=product_id,
            currency=currency,
            unit_amount=unit_amount,
            type=type,
            pricing_model=pricing_model,
            interval=interval,
            interval_count=interval_count,
            trial_period_days=trial_period_days,
            minimum_amount=minimum_amount,
            maximum_amount=maximum_amount,
            suggested_amount=suggested_amount,
            preset_amounts=preset_amounts,
            nickname=nickname,
            lookup_key=lookup_key,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Price:
        """
        Retrieves a price by its ID.

        Parameters
        ----------
        id : str
            Price ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Price
            Price details

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.prices.retrieve(
            id="id",
        )
        """
        _response = self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    def archive(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Price:
        """
        Archives a price by setting active to false. The price remains in the system but cannot be used for new purchases.

        Parameters
        ----------
        id : str
            Price ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Price
            Price archived successfully

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.prices.archive(
            id="id",
        )
        """
        _response = self._raw_client.archive(id, request_options=request_options)
        return _response.data

    def update(
        self,
        id: str,
        *,
        active: typing.Optional[bool] = OMIT,
        nickname: typing.Optional[str] = OMIT,
        lookup_key: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Price:
        """
        Updates a price. Only certain fields can be updated (active, nickname, lookup_key, metadata). Core pricing fields are immutable.

        Parameters
        ----------
        id : str
            Price ID

        active : typing.Optional[bool]
            Whether the price is active

        nickname : typing.Optional[str]
            Display name

        lookup_key : typing.Optional[str]
            Stable key for API lookups

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom key-value metadata

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Price
            Price updated successfully

        Examples
        --------
        from revkeen import RevKeen

        client = RevKeen(
            api_key="YOUR_API_KEY",
        )
        client.prices.update(
            id="id",
        )
        """
        _response = self._raw_client.update(
            id,
            active=active,
            nickname=nickname,
            lookup_key=lookup_key,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data


class AsyncPricesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawPricesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawPricesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawPricesClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        product_id: typing.Optional[str] = None,
        type: typing.Optional[PricesListRequestType] = None,
        pricing_model: typing.Optional[PricesListRequestPricingModel] = None,
        active: typing.Optional[PricesListRequestActive] = None,
        currency: typing.Optional[str] = None,
        lookup_key: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        starting_after: typing.Optional[str] = None,
        ending_before: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PriceListResponse:
        """
        List all prices for the authenticated merchant. Supports filtering and cursor-based pagination.

        Parameters
        ----------
        product_id : typing.Optional[str]
            Filter by product ID

        type : typing.Optional[PricesListRequestType]
            Filter by price type

        pricing_model : typing.Optional[PricesListRequestPricingModel]
            Filter by pricing model

        active : typing.Optional[PricesListRequestActive]
            Filter by active status

        currency : typing.Optional[str]
            Filter by currency

        lookup_key : typing.Optional[str]
            Filter by lookup key

        limit : typing.Optional[int]
            Number of results (1-100)

        starting_after : typing.Optional[str]
            Cursor for pagination (ID to start after)

        ending_before : typing.Optional[str]
            Cursor for pagination (ID to end before)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PriceListResponse
            List of prices

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.prices.list()


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            product_id=product_id,
            type=type,
            pricing_model=pricing_model,
            active=active,
            currency=currency,
            lookup_key=lookup_key,
            limit=limit,
            starting_after=starting_after,
            ending_before=ending_before,
            request_options=request_options,
        )
        return _response.data

    async def create(
        self,
        *,
        product_id: str,
        currency: typing.Optional[str] = OMIT,
        unit_amount: typing.Optional[int] = OMIT,
        type: typing.Optional[CreatePriceRequestType] = OMIT,
        pricing_model: typing.Optional[CreatePriceRequestPricingModel] = OMIT,
        interval: typing.Optional[CreatePriceRequestInterval] = OMIT,
        interval_count: typing.Optional[int] = OMIT,
        trial_period_days: typing.Optional[int] = OMIT,
        minimum_amount: typing.Optional[int] = OMIT,
        maximum_amount: typing.Optional[int] = OMIT,
        suggested_amount: typing.Optional[int] = OMIT,
        preset_amounts: typing.Optional[typing.Sequence[int]] = OMIT,
        nickname: typing.Optional[str] = OMIT,
        lookup_key: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Price:
        """
        Creates a new price for a product. Supports fixed, pay-what-you-want, and free pricing models.

        Parameters
        ----------
        product_id : str
            ID of the product this price belongs to

        currency : typing.Optional[str]
            Three-letter ISO currency code

        unit_amount : typing.Optional[int]
            Price in minor units (cents). Required for fixed pricing.

        type : typing.Optional[CreatePriceRequestType]
            Price type

        pricing_model : typing.Optional[CreatePriceRequestPricingModel]
            Pricing model

        interval : typing.Optional[CreatePriceRequestInterval]
            Billing interval (required for recurring)

        interval_count : typing.Optional[int]
            Number of intervals between billings (1-12)

        trial_period_days : typing.Optional[int]
            Trial period in days (0-730)

        minimum_amount : typing.Optional[int]
            PWYW: minimum amount in cents

        maximum_amount : typing.Optional[int]
            PWYW: maximum amount in cents

        suggested_amount : typing.Optional[int]
            PWYW: suggested amount in cents

        preset_amounts : typing.Optional[typing.Sequence[int]]
            PWYW: quick-select amounts (max 10)

        nickname : typing.Optional[str]
            Display name (e.g., 'Monthly', 'Annual')

        lookup_key : typing.Optional[str]
            Stable key for API lookups

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom key-value metadata

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Price
            Price created successfully

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.prices.create(
                product_id="product_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            product_id=product_id,
            currency=currency,
            unit_amount=unit_amount,
            type=type,
            pricing_model=pricing_model,
            interval=interval,
            interval_count=interval_count,
            trial_period_days=trial_period_days,
            minimum_amount=minimum_amount,
            maximum_amount=maximum_amount,
            suggested_amount=suggested_amount,
            preset_amounts=preset_amounts,
            nickname=nickname,
            lookup_key=lookup_key,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Price:
        """
        Retrieves a price by its ID.

        Parameters
        ----------
        id : str
            Price ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Price
            Price details

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.prices.retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(id, request_options=request_options)
        return _response.data

    async def archive(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Price:
        """
        Archives a price by setting active to false. The price remains in the system but cannot be used for new purchases.

        Parameters
        ----------
        id : str
            Price ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Price
            Price archived successfully

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.prices.archive(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.archive(id, request_options=request_options)
        return _response.data

    async def update(
        self,
        id: str,
        *,
        active: typing.Optional[bool] = OMIT,
        nickname: typing.Optional[str] = OMIT,
        lookup_key: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Price:
        """
        Updates a price. Only certain fields can be updated (active, nickname, lookup_key, metadata). Core pricing fields are immutable.

        Parameters
        ----------
        id : str
            Price ID

        active : typing.Optional[bool]
            Whether the price is active

        nickname : typing.Optional[str]
            Display name

        lookup_key : typing.Optional[str]
            Stable key for API lookups

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom key-value metadata

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Price
            Price updated successfully

        Examples
        --------
        import asyncio

        from revkeen import AsyncRevKeen

        client = AsyncRevKeen(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.prices.update(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            id,
            active=active,
            nickname=nickname,
            lookup_key=lookup_key,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data
